{% extends 'base.html.twig' %}

{% block title %}{{ episode.title }}{% endblock %}

{% block body %}
    <div class="bg-white">
        <h1>Modifier {{ episode.title }}</h1>

        {{ include('episode/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('episode_index') }}">Retour à la liste</a>

        {{ include('episode/_delete_form.html.twig') }}
    </div>
{% endblock %}
