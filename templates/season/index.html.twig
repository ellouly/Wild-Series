{% extends 'base.html.twig' %}

{% block title %}Liste des saisons{% endblock %}

{% block body %}
    <h1>Liste des saisons</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom de la série</th>
                <th>Année</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for season in seasons %}
                <tr></tr>
                <td>{{ season.program.title }}</td>
                <td>{{ season.year }}</td>
                <td>{{ season.description }}</td>
                <td>
                    <a href="{{ path('season_show', {'id': season.id}) }}">Accéder</a>
                    <a href="{{ path('season_edit', {'id': season.id}) }}">Modifier</a>
                </td>
            </td>
        {% else %}
            <tr>
                <td colspan="4">Pas d'article trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('season_new') }}">Créer une nouvelle saison</a>
{% endblock %}
