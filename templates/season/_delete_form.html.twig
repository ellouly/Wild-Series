<form method="post" action="{{ path('season_delete', {'id': season.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet article ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="btn btn-light m-3">Supprimer</button>
</form>
