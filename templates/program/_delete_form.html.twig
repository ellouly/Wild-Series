<form method="post" action="{{ path('program_delete', {'slug': program.slug}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet article ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.slug) }}">
    <button class="btn btn-light m-3">Supprimer</button>
</form>
