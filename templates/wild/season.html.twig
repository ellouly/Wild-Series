{% extends 'base.html.twig' %}

{% block title %}Saisons{% endblock %}

{% block body %}

    <div>
            <h2>Saison {{seasons.id}} de {{program.title }}</h2>
            <p>{{ seasons.description }}</p>
    </div>
    <div>
        <h3>Liste des épisodes</h3>
            {% for episode in episodes %}
                <a href="">Episode {{ episode.number }} : {{ episode.title }}</a><br>
            {% endfor %}
    </div>

{% endblock %}
